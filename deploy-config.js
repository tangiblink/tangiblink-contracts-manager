// Deployment variables are defined here.

const deployConfig = {
  initializer: {
    baseURL: "https://tangiblink.io/" || "UNSET",
    mintCostUsd: "0.5",
    latestRoundData: "87526400",
    feePercent: "1",
  },
}

module.exports = {
  deployConfig,
}
